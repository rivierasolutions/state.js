{
  "devDependencies": {
    "esbuild": "^0.27.2",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "serve": "http-server -p 5500 -c-1",
    "build": "esbuild src/state.js --bundle --minify --sourcemap --outfile=dist/state.min.js",
    "watch": "esbuild src/state.js --bundle --outfile=dist/state.min.js --watch"
  }
}
